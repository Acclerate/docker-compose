services:
  aria2:
    container_name: aria2
    image: hurlenko/aria2-ariang:latest
    restart: unless-stopped
    ports:
      - 51973:8080   # AriaNg Web UI
      - 52871:6800   # Aria2 RPC 端口
    environment:
      - PUID=1000
      - PGID=1000
      - ARIA2RPCPORT=6800
      - RPC_SECRET=your_token_here
    volumes:
      # 下载目录建议使用独立子目录，避免启动时递归 chown 扫描整个 Downloads
      - D:\Downloads\aria2-data:/aria2/data
      # 配置文件持久化
      - ./config:/aria2/conf
